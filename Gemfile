# gem file
source 'https://rubygems.org'

gem 'jekyll' # jekyll, with no specific version

# if jekyll error appears for webrick
# gem "webrick"

# gem plugins
group :jekyll_plugins do
  gem "jekyll-sitemap"
  gem "jekyll-paginate"
  gem "jemoji"
end
